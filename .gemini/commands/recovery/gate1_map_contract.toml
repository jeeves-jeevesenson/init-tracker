description = "Gate 1 recovery: restore DM map and dmcontrol surface contract without mixing gates."

prompt = """
You are running Production Recovery Gate 1 for init-tracker.

Read first:
- GEMINI.md
- docs/production_recovery_living_doc_20260526.md
- docs/agent_ops/gemini_recovery_workflow.md

Gate:
- Gate 1: Map Surface Contract Restoration

Goal:
- DM can reliably use `/dm/map` and `/dmcontrol`.

Allowed files from the recovery doc:
- dnd_initative_tracker.py
- assets/web/dmcontrol/index.html
- tests/test_dm_tactical_map_routes.py for Gate 1 test-contract repair
- docs/production_recovery_living_doc_20260526.md
- docs/runtime_reports/gate1_map_surface_contract_*.md

Forbidden scope:
- No spell engine changes.
- No combat service changes.
- No performance flag work.
- No resources/rest/pact work.
- No ships, surfaces, boarding, object, or experimental quarantine work.
- No Gate 2, Gate 3, Gate 4, or Gate 5 work.
- No production SSH, deploy, restart, topology change, commit, or push unless
  explicitly instructed.

Required unit test from the recovery doc:
- python3 -m unittest tests.test_dm_tactical_map_routes

Required browser smoke from the recovery doc:
- Move monster in `/dmcontrol`.
- Verify grid tokens in `/dm/map`.

Validation:
- Run `scripts/agent_gate_validate.sh gate1-map`.
- If `assets/web/dmcontrol/index.html` changed, the inline JavaScript syntax
  check for that asset is mandatory and must pass before claiming readiness.

Hard rules:
- Read the active gate before code changes.
- Do not mix gates.
- Do not edit files outside the active gate without stopping and reporting.
- Do not claim browser readiness from Python tests alone.

Final report must include:
- files inspected
- files changed
- exact Gate 1 branches/routes/contracts touched
- tests run and results
- JS syntax-check command/result if `assets/web/dmcontrol/index.html` changed
- browser smoke status and notes
- remaining risks
- git status --short

Validation discipline:
- Required gate validation is `scripts/agent_gate_validate.sh <gate-id>`.
- Do not run broad extra unittest classes after required validation passes.
- Extra tests are allowed only when targeted to a specific failure, timeout-bounded, and named in the final report.
- Known websocket tests must never be run without timeout.
- If required validation passes, stop and report; do not keep searching for more confidence.
- Browser smoke is developer-owned and is not replaced by extra Python tests.
"""
