# Workflow: spell pass validation

Run:

    scripts/agy/validate_spell_pass.sh

Then run any task-specific tests.

Pass requires:
- no syntax errors
- tests pass
- no warnings
- no unawaited coroutine warnings
- no hidden baseline failure claims
